# Windows/WSL metadata
*Zone.Identifier

# LaTeX build artifacts (paper/)
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.out
*.synctex.gz
paper/*.log

# Paper drafts and heavy binary assets (PDFs, .ai sources, IEEE template zip) —
# kept locally, not tracked in git to avoid repo bloat
/paper/
IEEE_Conference_Template__JFC_.zip
